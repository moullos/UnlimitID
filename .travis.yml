language: python

python:
  - "2.7"
# command to install dependencies
install: "pip install ."
# command to run tests
script: pytest tests

branches:
  only:
    - master

notifications:
    email: false
